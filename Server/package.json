{
  "name": "todomvc-ws-server",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run compile",
    "prepublish": "npm run compile",
    "compile": "babel src/index.js > dist/index.js",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ws": "^0.8.0"
},
  "devDependencies": {
    "babel": "^5.8.23"
  }
}
